"make" to compile main.exe
"make run" to run
"make clean" to remove .o files and tilda files
"make clear" to remove .o files, tilda files, and main.exe
"make view" to run and display the resulting image instead of saving

suggested uses:
"make run clean"
"make view clean"
"make run clear"
"make view clear"